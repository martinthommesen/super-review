# This workbench and the shipped skill need no runtime secrets or environment
# variables. Copy to `.env` only if you add local experimentation keys; do not
# commit `.env`. CI secret scanning uses gitleaks (see SECURITY.md).
