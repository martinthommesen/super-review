{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c 'import json,os,sys; d=json.load(sys.stdin); p=(d.get(\"tool_input\") or {}).get(\"file_path\") or \"\"; r=os.environ.get(\"CLAUDE_PROJECT_DIR\",\"\"); p=p[len(r)+1:] if r and p.startswith(r+\"/\") else p; sys.stdout.write(json.dumps({\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"Blocked by repo policy: dist/ is generated (edit src/super-review/ and run make build); docs/ORIGINAL_REVIEW_PROMPT.md is archival provenance.\"}})) if p.startswith(\"dist/\") or p.startswith(\"docs/ORIGINAL_REVIEW_PROMPT.md\") else None'",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
