# Development-only tooling for the CI pip path and non-uv setups (the lint and
# spec CI jobs install from this file). Local uv setups get the same tools from
# [dependency-groups].dev in pyproject.toml via `uv sync --dev`.
# Runtime scripts in the skill remain Python-standard-library-only.
# Pins must match [dependency-groups].dev in pyproject.toml.
coverage==7.15.4
pre-commit==4.6.2
ruff==0.16.4
skills-ref==0.1.1
ty==0.0.75
