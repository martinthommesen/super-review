# Development-only tooling used by `make spec`, `make lint`, `make coverage`, and CI.
# Runtime scripts in the skill remain Python-standard-library-only.
# Pins must match [dependency-groups].dev in pyproject.toml.
coverage==7.15.2
pre-commit==4.6.1
ruff==0.15.22
skills-ref==0.1.1
ty==0.0.62
